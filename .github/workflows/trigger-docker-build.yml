name: Trigger Docker Build

# We want to be able to trigger this workflow manually
# and also run it once a week on Sunday
on:
  schedule:
    - cron: '0 0 * * 0'
  workflow_dispatch:

jobs:
  trigger-circleci:
    runs-on: ubuntu-latest
    steps:
      - name: Build Docker Images
        id: docker-build
        uses: CircleCI-Public/trigger-circleci-pipeline-action@v1.2.0
        env:
          CCI_TOKEN: ${{ secrets.CCI_TOKEN }}
